export interface NavLink {
  label: string
  href: string
}

export interface Language {
  code: string
  label: string
  flag: string
}

export interface Category {
  id: string
  name: string
  itemCount: number
  icon: string
  bgColor: string
  iconColor: string
}
